# Decision Log

## Pregled odluka

| ID odluke | Datum | Kratak naziv odluke | Opis problema ili pitanja | Razmatrane opcije | Odabrana opcija | Razlog izbora | Posljedice odluke | Status |
|---|---|---|---|---|---|---|---|---|
| DL-5-01 | 2026-04-29 | Zadržavanje postojećeg tehnološkog stacka | Trebalo je potvrditi da li tim ostaje na tehnološkom stacku definisanom u skeletonu projekta | Promjena stacka; zadržavanje Spring Boot + React + PostgreSQL | Zadržavanje postojećeg stacka | Stack je već bio definisan i djelimično postavljen u projektu, što omogućava kontinuitet rada | Omogućen je brži početak implementacije bez dodatnih migracija i promjena arhitekture | Aktivna |
| DL-5-02 | 2026-04-29 | Korištenje PostgreSQL baze za razvoj i testiranje | Trebalo je odabrati bazu podataka za prvi funkcionalni inkrement sistema | In-memory baza; lokalni PostgreSQL; udaljena baza | PostgreSQL | PostgreSQL je usklađen sa planiranom arhitekturom sistema i omogućava realističnije testiranje rada aplikacije | Potrebno je održavati konfiguraciju konekcije i voditi računa o parametrima okruženja | Aktivna |
| DL-5-03 | 2026-04-29 | Uvođenje testnih API endpoint-a za validaciju rada backenda | Trebalo je odrediti način provjere da backend i API sloj funkcionišu prije pune implementacije svih modula | Odmah puna implementacija svih modula; privremeni testni endpoint-i | Uvođenje testnih endpoint-a kao što su `GET /api/users` i `GET /api/auth/ping` | Omogućena je brza validacija komunikacije između klijenata i servera | Kasnije je potrebno proširiti i standardizovati endpoint-e za potpunu funkcionalnost sistema | Aktivna |
| DL-5-04 | 2026-04-30 | Uvođenje JWT autentifikacije u ranom inkrementu | Trebalo je odlučiti da li autentifikaciju ostaviti za kasniji sprint ili je uvesti ranije | Odgađanje autentifikacije; uvođenje osnovnog JWT login toka | Uvođenje osnovne JWT autentifikacije | Login i identitet korisnika bili su potrebni za realističnije testiranje sistema i pripremu narednih funkcionalnosti | Potrebno je dodatno proširiti autorizaciju i zaštitu ruta u narednim sprintovima | Aktivna |
| DL-5-05 | 2026-04-30 | Korištenje env varijable za backend API adresu | Trebalo je definisati kako frontend čita adresu backend servisa u različitim okruženjima | Hardkodiran URL; env varijable | Env varijabla za API base URL | Omogućava jednostavno razlikovanje lokalnog i produkcijskog okruženja bez izmjene izvornog koda | Potrebno je pravilno održavati deployment postavke na Vercelu i drugim okruženjima | Aktivna |
| DL-5-06 | 2026-04-30 | Deployment frontenda i backenda na odvojene servise | Trebalo je odabrati način javnog objavljivanja aplikacije za demonstraciju | Lokalno pokretanje; zajednički hosting; odvojeni frontend i backend servisi | Frontend na Vercelu, backend i baza na Renderu | Ovakav pristup je jednostavan za demonstraciju, prati stvarnu web arhitekturu i omogućava javni pristup aplikaciji | Potrebno je održavati CORS postavke, environment varijable i međusobnu povezanost servisa | Aktivna |
| DL-5-07 | 2026-04-30 | Uvođenje CORS konfiguracije za komunikaciju između servisa | Trebalo je omogućiti sigurnu komunikaciju između frontenda hostanog na Vercelu i backenda hostanog na Renderu | Bez CORS konfiguracije; potpuno otvoren CORS; ograničen CORS po known origin-u | Ograničen CORS sa eksplicitno dozvoljenim frontend origin-om | Time je omogućena kontrolisana i sigurna komunikacija između klijentske i serverske aplikacije | Pri svakoj promjeni frontend domene potrebno je ažurirati backend konfiguraciju | Aktivna |
